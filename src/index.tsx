import * as React from 'react';
import * as ReactDOM from 'react-dom';

const App = () => {
  return <h1>Hello, welcome to Meetup Ng!</h1>;
};

// render the app to the page
ReactDOM.render(<App />, document.getElementById('app'));
